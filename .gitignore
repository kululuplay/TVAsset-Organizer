# Signing keystores and their base64 exports must NEVER be committed.
*.jks
*.keystore
*.b64
keystore.properties

# Local service configuration and credentials.
.env
.env.*
!.env.example

# Node (crash-receiver backend)
node_modules/
npm-debug.log*
