<?xml version="1.0" encoding="utf-8"?>
<!-- Poster card for the VOD / series grid. Fixed poster size, focusable for
     D-pad navigation, with name + rating below the artwork. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/poster_width"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_card_selectable"
    android:focusable="true"
    android:focusableInTouchMode="false"
    android:padding="@dimen/space_xs"
    android:layout_margin="@dimen/space_xs">

    <ImageView
        android:id="@+id/posterImage"
        android:layout_width="match_parent"
        android:layout_height="@dimen/poster_height"
        android:scaleType="centerCrop"
        android:contentDescription="@string/nav_movies" />

    <TextView
        android:id="@+id/posterName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_xs"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/text_caption"
        android:maxLines="1"
        android:ellipsize="end" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/posterStar"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:src="@drawable/ic_star"
            android:layout_marginEnd="@dimen/space_xs"
            android:visibility="gone"
            android:contentDescription="@string/detail_rating" />

        <TextView
            android:id="@+id/posterRating"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_secondary"
            android:textSize="@dimen/text_caption"
            android:maxLines="1" />
    </LinearLayout>
</LinearLayout>
