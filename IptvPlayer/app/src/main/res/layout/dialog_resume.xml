<?xml version="1.0" encoding="utf-8"?>
<!-- Modern "resume playback" prompt. Shows the exact stopped-at timestamp in a
     highlighted chip; primary CTA = resume, secondary = start over. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="560dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_dialog"
    android:padding="@dimen/space_l">

    <TextView
        android:id="@+id/dialogTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/resume_prompt_title"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/text_title"
        android:textStyle="bold" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_s"
        android:text="@string/resume_prompt_message"
        android:textColor="@color/text_secondary"
        android:textSize="@dimen/text_body" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_l"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_time_chip"
        android:paddingHorizontal="@dimen/space_m"
        android:paddingVertical="@dimen/space_m">

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_play"
            android:contentDescription="@null"
            app:tint="@color/accent" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/space_m"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/resume_prompt_position_label"
                android:textColor="@color/text_muted"
                android:textSize="@dimen/text_caption"
                android:textAllCaps="true"
                android:letterSpacing="0.05" />

            <TextView
                android:id="@+id/resumeTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="00:00"
                android:textColor="@color/text_primary"
                android:textSize="@dimen/text_title"
                android:textStyle="bold" />
        </LinearLayout>
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_l"
        android:orientation="horizontal"
        android:gravity="end">

        <TextView
            android:id="@+id/startOverButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/space_m"
            android:background="@drawable/bg_button"
            android:drawableStart="@drawable/ic_restart"
            android:drawablePadding="@dimen/space_s"
            android:focusable="true"
            android:gravity="center"
            android:minWidth="160dp"
            android:paddingHorizontal="@dimen/space_l"
            android:paddingVertical="@dimen/space_m"
            android:text="@string/resume_from_start"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_body" />

        <TextView
            android:id="@+id/resumeButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_button_primary"
            android:drawableStart="@drawable/ic_play"
            android:drawablePadding="@dimen/space_s"
            android:focusable="true"
            android:gravity="center"
            android:minWidth="160dp"
            android:paddingHorizontal="@dimen/space_l"
            android:paddingVertical="@dimen/space_m"
            android:text="@string/resume_continue"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_body"
            android:textStyle="bold" />
    </LinearLayout>
</LinearLayout>
