*.iml
.gradle/
.kotlin/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/

# Signing keystores and their base64 exports must NEVER be committed.
*.jks
*.keystore
*.b64
keystore.properties

# Locally built Media3 FFmpeg extension (CI restores it from cache; never commit binaries).
app/libs/media3-decoder-ffmpeg-release.aar
app/libs/media3-decoder-ffmpeg-release.txt
