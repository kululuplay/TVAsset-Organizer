The RV16 chroma fix did NOT work and the log proves WHY. On the Xiaomi Amlogic stick with decoder=HARDWARE, the green screen persists and the libVLC decoder output is STILL output: 21 Biplanar 4:2:0 Y/UV — it was NEVER converted to packed RGB. So the --android-display-chroma=RV16 lever had zero effect on the hardware path. Reason: with hardware/MediaCodec decode, frames are opaque native buffers rendered directly by the Amlogic compositor onto its video underlay plane; they never pass through VLC's software chroma converter, so --android-display-chroma is architecturally ineffective for hardware decode. It only affects software decode. Also, you DISABLED direct rendering "so RV16 conversion works" — but RV16 isn't working anyway, and disabling MediaCodec direct rendering on the hardware path is likely harmful, because Amlogic hardware frames are meant to render directly to the surface. Do the following:

Remove the ineffective RV16/no-direct-rendering hack from the hardware path. Re-enable MediaCodec direct rendering for hardware decode so frames render directly to the SurfaceView (this is how Amlogic displays hardware video). The chroma conversion approach is the wrong tool for hardware decode — drop it.
PRIMARY TEST — try the ExoPlayer/Media3 engine on hardware for this Amlogic device. The app already has both engines. Amlogic's native pipeline is MediaCodec→underlay plane, which is exactly how ExoPlayer renders (it hands the SurfaceView directly to MediaCodec). libVLC's android_display vout struggles on Amlogic where ExoPlayer often works. Make it possible to select/route the ExoPlayer engine in HARDWARE mode and test whether it shows real video (no green) on the Xiaomi stick. This is the most likely fix — prioritize it.
Keep the audio path untouched (it's correct: PCM stereo, passthrough=false).
Do NOT just set the default back to Software to hide the problem — Software stutters on this weak stick, which is the original complaint. The goal remains: HARDWARE decode shows real video with no green screen.

VERIFY on the Xiaomi Amlogic stick: with hardware decode (whichever engine works), the channel must display real video, not green. Report which engine (libVLC vs ExoPlayer) succeeds on Amlogic so we can route per-engine/per-device.