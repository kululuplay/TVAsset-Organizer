NEW real-device log analysis. Important: the previous fix made things WORSE — there is now a restart loop. The device is Amlogic (OMX.amlogic.avc.decoder.awesome2). Read this carefully:
WHAT THE LOG PROVES:

The hardware decoder WORKS. It initializes, reaches kDecoding, and allocates output buffers (setOutputBuf ... fd:28..89). So HW decode capability is NOT the problem. Do NOT chase software fallback as the primary fix.
The green screen is purely a VIDEO OUTPUT WINDOW problem, still unfixed. These errors repeat constantly: libvlc window: request 0/1/3 not implemented, libvlc vout display: can't get Subtitles Surface, NdkMediaCodec: sf error code: -38. The libVLC Android video window (IVLCVout / AWindow) is still NOT properly wired, so decoded frames are never displayed → green screen.
A RESTART LOOP was introduced (this is the freezing). This repeats every few seconds: libvlc decoder: dequeue_in timeout: no input available for 2secs immediately followed by PlaybackLog: [Controller] play mode=VLC start=VLC_HW. The player starts, gets starved of input for 2s, then the app restarts playback, forever. This loop is the freeze/stutter the user sees.
A new regression: the decoder now reports mIsSurfacTexture 1 (it was 0 before). The video is now going through a SurfaceTexture/TextureView path, which breaks the Amlogic hardware video plane.

FIX — in this priority order:
A. STOP THE RESTART LOOP FIRST (most urgent). Find the retry/fallback logic that was recently added and fix it: a 2-second dequeue_in timeout or a transient decoder event must NOT trigger an immediate full playback restart. Remove the tight restart loop. Allow the stream to buffer; only retry after a real, confirmed failure, with backoff, and at most once or twice, never in a continuous loop. The repeated [Controller] play calls must stop.
B. REVERT TO SURFACEVIEW (fix the SurfaceTexture regression). Force useTextureView = false everywhere. The log shows mIsSurfacTexture 1 — the video must render on a plain SurfaceView, because Amlogic renders hardware-decoded video on an underlay plane that a TextureView/SurfaceTexture cannot display.
C. WIRE THE libVLC VIDEO WINDOW CORRECTLY (fixes the green screen). The libvlc window: request not implemented spam means the AWindow/IVLCVout window handler is not set up. Use libVLC's official path: a VLCVideoLayout in the layout, and mediaPlayer.attachViews(vlcVideoLayout, null, false, false) (subtitles=false, useTextureView=false) so libVLC installs its own window handler that answers the window/resize requests. Do NOT hand-attach a bare Surface that ignores these requests. Attach exactly once; detach on stop. After this, the request ... not implemented and sf error code: -38 errors must be gone and video must show.
D. Audio is fine — do not touch it. The log shows correct PCM stereo output (AudioTrack format 0x1 channelMask 0x3, AAC channels: 2), passthrough=false. Leave the audio path as is.
VERIFY on the real Amlogic stick: after the fix, the log must show NO libvlc window: request not implemented, NO repeating [Controller] play restart loop, NO dequeue_in timeout cascade, and the 1080i channel must display real video continuously with audio.